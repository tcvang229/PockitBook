namespace PockitBook;

/// <summary>
/// Simply a file that exposes constant values.
/// </summary>
public static class Constants
{
    /// <summary>
    /// Enum values to be used to distictive about the different views available in the app.
    /// </summary>
    public enum AppViews
    {
        HomeView,
        BillDetailsView,
        AccountProjectionView
    };
}